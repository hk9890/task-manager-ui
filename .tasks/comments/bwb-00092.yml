---
id: a2d2f0zd
author: Hans Kohlreiter
created: 2026-06-04T16:34:52Z
body: |-
  Implemented. Content pane header reworked to dashboard-styled tokens (option chosen by user).

  New header (top→bottom): compact meta row [type · priority · status colored like the
  board, full id muted] → title (plain) → thin full-width rule (─, muted) → Description + body.
  Reuses renderhelpers.Compact*Styled + styles.IssueIDMutedStyle (same colors as dashboard/board).

  Files:
  - internal/ui/details/details.go: renderContentPaneLines header rebuilt; added
    contentHeaderMetaRow (with width-bounded plain fallback) and contentHeaderRule helpers;
    added renderhelpers import. Net header line count unchanged (4), so skeleton math holds.
  - Regenerated goldens: internal/ui/details/testdata/* and internal/ui/search/testdata/*
    (search preview pane uses the same content renderer).
  - internal/mode/details/model_test.go: updated TestModelDetailScrollMovesViewportForLongContent
    initial/home assertions to the new top line (meta row) since the title now sits one line lower.

  Gates: vet, lint (0 issues), fmt:check, guardrails, full `go test ./...` — all PASS.

  Out of scope / noted: the search no-selection placeholder preview now reflows to "? P0 (NO (none)"
  (was "(none) · (none) · (unknown)") — pre-existing empty-state ugliness, not part of this header
  task. renderCompact (narrow fallback variant) keeps its own minimal header.
