---
id: bwb-00769
title: 'Acceptance Review: paginated Done column'
status: closed
type: task
priority: 1
assignee: hans.kohlreiter@dynatrace.com
creator: Hans Kohlreiter
labels:
  - beads:beads-workbench-vtvb.12
parent: bwb-00016
blocked_by:
  - bwb-00747
  - bwb-00746
  - bwb-00708
  - bwb-00745
  - bwb-00744
  - bwb-00643
created: 2026-05-28T10:44:12Z
updated: 2026-05-28T14:54:37Z
closed: 2026-05-28T14:54:37Z
close_reason: done
---

Acceptance review for the paginated Done column epic (vtvb). Runs as the closure gate per the project's epic-completion pattern (see iwvm.9, fbea.8).

## Pre-flight checks

1. All sibling vtvb tasks closed (`bd list --parent beads-workbench-vtvb --status open` returns only this AR task).
2. `mise run quality` green on a clean checkout (vet + lint + guardrails + unit + integration).
3. CHANGELOG.md updated with the v0.5.x entry describing the new pagination.

## Automated evidence

Run from a clean checkout, capture output:

- `mise run quality` → exit 0.
- `go test ./internal/repository/contract -tags integration -run TestRepositoryContract -v` → all sub-tests pass including the new `PaginatedClosedFetch` scenario (vtvb.9).
- Argv pin test: `go test ./internal/repository/beads -run TestDashboardClosedOffsetArgv -v` → pass (vtvb.2).
- Board-model tests: `go test ./internal/mode/board -run 'TestDoneLoadMore|TestDoneInFlightGuard|TestDoneRefreshReset' -v` → pass (vtvb.6/.7/.8).
- Composer test: `go test ./internal/dashboard -run 'TestComposerMergesDone' -v` → pass (vtvb.5).
- Golden tests: `go test ./internal/ui/board -run TestDoneGoldens -v` → pass (vtvb.10).

## Manual runtime verification (MANDATORY — target: /home/hans/dev/github/dtctl-test)

The user explicitly requested a full manual test against the dtctl-test repository because it has the largest closed population locally. Run the procedure documented in `docs/RUNTIME_UI_VERIFICATION.md` (added by vtvb.11):

1. Build bwb fresh: `mise run build`.
2. Open a terminal at height ≤ 30 rows.
3. `(cd /home/hans/dev/github/dtctl-test && BD_NON_INTERACTIVE=1 /tmp/bwb)`
4. Walk every step in the runbook. Capture pass/fail per the runbook's table.
5. ALSO smoke-test `b38b.4`: focus Ready column, press `j` ~50 times, confirm `›` chevron stays visible and header reads `N of 86` (where N is the rendered window size) — not just `86`.

Record the manual session as a comment on this issue with: the terminal dimensions used, the exact final Done header observed at maximum depth, and a screenshot or transcript excerpt showing the chevron-in-window property.

## Acceptance criteria (mirror of vtvb description)

- [ ] `DashboardOptions.ClosedOffset` exists; default `0` behaves identically to today.
- [ ] All three repository impls (memory, beads, caching) honor `ClosedOffset` and pass the parity scenario.
- [ ] `bd query --offset M` argv is pinned in a test.
- [ ] Board model dispatches at most one `loadMoreClosedCmd` per threshold crossing; in-flight guard verified by `DelayedFakeRepository`.
- [ ] Done header `N of M` grows correctly as additional pages load; resets on `r`.
- [ ] Cursor remains inside the rendered window when scrolling deep (relies on `b38b.4`).
- [ ] Runtime runbook procedure exists, targets dtctl-test, and passes.
- [ ] `mise run quality` green.

## Close criteria

If all evidence above lands and the manual runbook session is captured as a comment with a PASS verdict, close vtvb. Otherwise file follow-up bugs from the orchestrator (not the verifier) and re-run the AR.
