---
id: bwb-00291
title: splitResponsiveLayoutHeights has unreachable defensive guard branch
status: closed
type: chore
priority: 4
assignee: hans.kohlreiter@dynatrace.com
creator: Hans Kohlreiter
labels:
  - area:cleanup
  - beads:beads-workbench-dy5u
created: 2026-05-19T15:39:50Z
updated: 2026-05-20T18:54:12Z
closed: 2026-05-20T18:54:12Z
close_reason: Closed
---

## Summary

The `bottom < 3` guard at `internal/ui/details/details.go:277-280` (in `splitResponsiveLayoutHeights`) appears to be unreachable for any valid input. Tracing all integer values of `total` in the range 7-9 (the only range where the prior shift logic could plausibly leave bottom < 3) shows the branch never fires — the shift logic already produces bottom >= 3.

Discovered during `beads-workbench-5s67` while raising coverage on this function from 38.9% → 88.9%. The remaining ~11% all sits in this guard.

## Acceptance

- [ ] Confirm the branch is genuinely unreachable (read+trace all integer inputs, or document a case that does reach it)
- [ ] If unreachable: delete the guard and its dead branch
- [ ] If reachable but rare: add a test case that exercises it and leave the guard

## Discovered while working on

`beads-workbench-5s67` (Test: responsive layout math). Filed by the orchestrator to avoid silently dropping the finding.
