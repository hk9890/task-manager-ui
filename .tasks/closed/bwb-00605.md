---
id: bwb-00605
title: 'Acceptance Review: Test suite quality: fix labeling, cover bd-1.0.4 workarounds, drop dead assertions'
status: closed
type: task
priority: 2
assignee: hans.kohlreiter@dynatrace.com
creator: Hans Kohlreiter
labels:
  - beads:beads-workbench-j5bg
parent: bwb-00101
blocked_by:
  - bwb-00446
  - bwb-00445
  - bwb-00449
  - bwb-00448
  - bwb-00450
  - bwb-00447
created: 2026-05-25T18:47:22Z
updated: 2026-05-26T06:58:02Z
closed: 2026-05-26T06:58:02Z
close_reason: |-
  All 6 acceptance criteria verified by verifier agent acd9329e1ba2b5001.

  (1) All 6 impl tasks confirmed closed.
  (2) mise run quality:fast: 0 lint, 0 vet, 1068 tests PASS.
  (3) mise run test:integration: 1217 tests, 15 expected skips, wall time 77.769s (under 80s gate; baseline ~165s before this epic).
  (4) New test file lean_writes_test.go: no <0 assertions. One dead guard in MODIFIED generate_test.go:387 (file existed pre-epic) logged as non-blocking follow-up beads-workbench-99bs.4.
  (5) No developer-specific paths in new/modified test files.
  (6) Conditional UI smoke triggered (c4la touched internal/app/model.go and services.go): PTY read-only nav PASS, PTY filter PASS, edit-flow teatests 3/3 PASS under -race.
---

## Description
Verify the epic outcome before closure.

The implementation tasks across this epic touch only `_test.go` files, `docs/TESTING.md`, and `scripts/` (delete). They do NOT modify production code paths. The plan-review tightened the original "manual binary-launch" gate to a conditional one (Q9): a manual UI smoke is only required if a tasker ended up touching non-`_test.go` production code (the c4la signal-channel fallback is the only plausible case).

## Acceptance Criteria
- [ ] All child implementation tasks closed
- [ ] Required tests/checks completed and passing
- [ ] No unresolved critical defects
- [ ] Follow-up issues filed for non-blocking gaps
- [ ] **Conditional UI smoke** — verifier runs `git diff --name-only main` and checks whether any non-`_test.go` file outside `docs/`, `scripts/`, and the `.beads/` JSONL was modified by this epic's commits:
  - If NO production code changed (expected case): verifier confirms this and SKIPS the manual binary launch. Record "no production code changed; manual smoke skipped per j5bg conditional gate" in the close comment.
  - If production code DID change (e.g. c4la took the signal-channel fallback): verifier launches the bwb binary per `docs/RUNTIME_UI_VERIFICATION.md`, navigates the issue list, applies a filter, opens an issue, runs the edit flow, observes the toast on success, and captures evidence (terminal log or screenshot).
- [ ] `mise run quality:fast` passes (full output captured)
- [ ] `mise run test:integration` passes (full output captured)
- [ ] No new test file added across this epic contains an assertion that cannot fail by construction (grep verification of '< 0' on duration/count types)
- [ ] No new test file references a developer-specific absolute path
