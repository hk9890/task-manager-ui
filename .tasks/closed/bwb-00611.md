---
id: bwb-00611
title: 'Acceptance Review: BeadsGateway contract verification'
status: closed
type: task
priority: 2
assignee: hans.kohlreiter@dynatrace.com
creator: Hans Kohlreiter
labels:
  - beads:beads-workbench-g93r
parent: bwb-00121
blocked_by:
  - bwb-00499
  - bwb-00501
  - bwb-00497
  - bwb-00503
  - bwb-00498
  - bwb-00502
  - bwb-00500
  - bwb-00504
created: 2026-05-17T21:49:14Z
updated: 2026-05-17T23:20:39Z
closed: 2026-05-17T23:20:39Z
close_reason: 'All AC met. 8 audit tasks closed with 4-section comments (re-posted after initial ''bd comments add - <heredoc>'' bug; now using -f flag). 23 drift findings filed and ALL closed (P1=1, P2=5, P3=14, P4=3). Both upstream workarounds re-verified STILL PRESENT in bd 1.0.4 — retained. py38 P1 production bug (ClearLabels --remove-labels) fixed; 5toe integration test prevents recurrence. 8 fake parity fixes landed with 2026-05-18 drift log entries. interface.go re-verification note added. Test suite: build, vet, unit (32 pkgs), integration (real bd) all PASS.'
---

## Description

Verify the outcomes of epic `beads-workbench-yspw` (BeadsGateway contract verification) before closing the epic.

This task is the gate — it should remain open until all 8 verification tasks are closed AND the post-audit cleanup is complete.

## Acceptance Criteria

- [ ] All 8 child verification tasks closed with audit comments posted:
  - `beads-workbench-ya4q` (Health)
  - `beads-workbench-oyb2` (Read-lists)
  - `beads-workbench-u53a` (Read-explain)
  - `beads-workbench-aww9` (Read-detail)
  - `beads-workbench-vhp2` (Read-search)
  - `beads-workbench-048i` (Read-counts)
  - `beads-workbench-r1m3` (Read-catalogs)
  - `beads-workbench-ibif` (Write)
- [ ] All drift findings tracked as `bug` tickets linked back to epic `beads-workbench-yspw` (zero silent drift across all 8 audits)
- [ ] If either active workaround is no longer needed (CloseIssue idempotency / Update --set-labels ""), a `chore` ticket to remove it exists
- [ ] Package-level audit timestamps in `internal/gateway/beads/interface.go` (currently "audit 2026-05-16" and "audit 2026-05-17") are refreshed to today OR a follow-up bug captures the doc-refresh
- [ ] Fake drift log in `internal/testing/fakes/doc.go` updated if any divergence was found and fixed
- [ ] Epic-level summary comment posted on `beads-workbench-yspw` listing:
  - Total clauses verified
  - Total drift findings filed
  - Status of the two active workarounds
  - Any test-coverage gaps that became their own follow-up tickets

## Instructions

1. Run `bd show beads-workbench-yspw` and `bd list --json --status closed` filtered for the 8 task IDs above — confirm all closed.
2. For each closed task, read the audit comment and verify it covers all 4 sections (contract / real-bd / tests / fake).
3. Cross-check that every DRIFT marking has a corresponding `bug` ticket.
4. Confirm the two workaround status checks (CloseIssue idempotency, Update --set-labels "") were performed in task `beads-workbench-ibif`.
5. Post the epic-level summary comment on `beads-workbench-yspw`.
6. Only then close this acceptance review task and the epic.
