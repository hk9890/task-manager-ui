---
id: bwb-00448
title: Sync docs/TESTING.md Tier 2 sections with actual contract harness
status: closed
type: chore
priority: 2
assignee: hans.kohlreiter@dynatrace.com
creator: Hans Kohlreiter
labels:
  - beads:beads-workbench-hqsk
parent: bwb-00101
created: 2026-05-25T18:45:47Z
updated: 2026-05-26T06:44:28Z
closed: 2026-05-26T06:44:28Z
close_reason: All acceptance criteria met (gate output and resolution captured in task comments). Cherry-picked onto main; mise run quality:fast and mise run test:integration both green on main after all 6 landed.
---

## Description
`docs/TESTING.md` Tier 2a and Tier 2b sections describe a contract-harness API that does not exist in the active source tree:

> *"Single parameterized function `RunReadContract(t, factory RepositoryFactory)`"*
> *"`TestRealRepositoryIssueLifecycleScenario` — create → update → comment → close"*
> *"`TestRealRepositoryLinksAndDepsScenario` — `bd link`, `bd dep relate`, `bd dep add`"*
> *"`TestRealRepositoryReadContract` wires it against the real `bd`-backed `beads.Repository`"*

`grep` returns nothing in the active tree for any of `RunReadContract`, `TestRealRepositoryReadContract`, `TestRealRepositoryIssueLifecycleScenario`, `TestRealRepositoryLinksAndDepsScenario`. They only exist inside `.claude/worktrees/5q6t/internal/gateway/beads/contract/` (an abandoned worktree).

The doc also cites the wrong package path: `internal/repository/beads/contract/` (lines 21 and 97) — that directory does NOT exist. The actual harness lives in `internal/repository/contract/parity_integration_test.go` as `TestRepositoryContract` with inline scenarios named `Scenario1/EmptyStore`, `Scenario8/...`, etc. — not the documented `RunReadContract` + scenario-split shape.

Any contributor reading the docs to find where their new mutating contract test goes will look for code that does not exist.

## Instructions
1. Read `internal/repository/contract/parity_integration_test.go` end-to-end. Note:
   - The entry-point function name and its signature (`TestRepositoryContract`)
   - The factory list wired in (confirm exact set, e.g. memory, beads, validating)
   - The scenario list with their numeric prefixes and titles, in the order they appear
   - The convention for adding a new scenario (sub-test name pattern, helper signatures)
2. Open `docs/TESTING.md` and locate ALL fictitious symbols and the wrong package path. The complete list (verified by grep before plan-review):
   - Line 21: package `internal/repository/beads/contract/` — does NOT exist; correct path is `internal/repository/contract/`
   - Line 22: `RunReadContract(t, factory RepositoryFactory)` — does NOT exist
   - Line 24: another `RunReadContract` reference
   - Line 31: `TestRealRepositoryIssueLifecycleScenario` — does NOT exist
   - Line 32: `TestRealRepositoryLinksAndDepsScenario` — does NOT exist
   - Line 41: another `RunReadContract` reference
   - Line 67: another `RunReadContract` reference
   - Line 69: `TestRealRepositoryReadContract` — does NOT exist
   - Line 97: another `internal/repository/beads/contract/` path reference
3. Rewrite the Tier 2 sections so every code symbol and package path resolves to a real file. The rewrite must:
   - Name the actual entry point `TestRepositoryContract`
   - Cite the actual file path `internal/repository/contract/parity_integration_test.go`
   - Cite the correct package path `internal/repository/contract/`
   - List the factories that are wired in
   - List the scenarios that currently exist (with their numeric prefixes)
   - Document where and how to add a new scenario
4. After rewriting, verify every code symbol and package path you mention by running:
   ```
   grep -rn "<symbol>" --exclude-dir=.claude --exclude-dir=node_modules .
   ```
   for each. Every reference must resolve to a real file or directory in the active tree.

## Files to Modify
- `docs/TESTING.md`

## Acceptance Criteria
- [ ] No fictitious symbols remain: `grep -rn "RunReadContract\|TestRealRepositoryReadContract\|TestRealRepositoryIssueLifecycleScenario\|TestRealRepositoryLinksAndDepsScenario" docs/` returns zero matches
- [ ] No reference to the non-existent path `internal/repository/beads/contract/` remains in `docs/`
- [ ] Every code symbol AND package path mentioned in the rewritten Tier 2 sections resolves to a file/dir in the active tree (excluding `.claude/worktrees/`)
- [ ] The doc names the actual file path and entry-point function (`internal/repository/contract/parity_integration_test.go`, `TestRepositoryContract`)
- [ ] A new contributor can follow the doc to add a new scenario without reading the source first
- [ ] `mise run quality:fast` passes
