---
id: bwb-00604
title: 'Acceptance Review: Address monitoring-data findings from log analysis'
status: closed
type: task
priority: 2
assignee: hans.kohlreiter@dynatrace.com
creator: Hans Kohlreiter
labels:
  - beads:beads-workbench-d2oj.7
parent: bwb-00099
blocked_by:
  - bwb-00440
  - bwb-00436
  - bwb-00435
  - bwb-00439
  - bwb-00438
  - bwb-00437
created: 2026-05-29T09:16:58Z
updated: 2026-05-29T11:19:52Z
closed: 2026-05-29T11:19:52Z
close_reason: Closed
---

## Description
Acceptance gate for epic beads-workbench-d2oj. Verify every monitoring-data follow-up task against its acceptance criteria AND the epic-level outcome before the epic is closed. Several findings are investigations/diagnostics rather than code fixes — verify the recorded decision or diagnostic, not just a diff. This task stays blocked until d2oj.1–.6 are closed.

## Verification Steps
1. Confirm every child implementation task is CLOSED with its acceptance criteria met:
   - d2oj.1 — leanDepsFromPayload returns non-nil empty BlockedBy; regression test added; fresh dev browse emits no BlockedByNotNil violations.
   - d2oj.2 — root cause of exit_code=-1 vc-status failures identified, OR a diagnostic WARN carrying the underlying exec error was added.
   - d2oj.3 — decision on dashboard fan-out p95 recorded (optimize-and-how, or accept-with-rationale).
   - d2oj.4 — cold start no longer emits dozens of wrong-schema-version cache WARN records; cache dir bounded.
   - d2oj.5 — locally built/run bwb logs a git-describe build_version, not bare 'dev'.
   - d2oj.6 — cold-load metric adjusted to reflect startup latency only, or documented as activity-inclusive.
2. Re-run the quality gate independently: `mise run test` and `mise run quality` (plus integration if relevant) — all green.
3. For the diagnostic/decision tasks (d2oj.2, .3, .6), confirm the decision/diagnostic is captured as a comment or doc/code change, not merely asserted.
4. Re-check the original log-analysis signals on a fresh dev build (scripts/analyze_dashboard_perf.py and the WARN grep): BlockedByNotNil noise, wrong-schema cache WARN spam, and bare build_version=dev no longer reproduce.
5. File any newly discovered defects as separate bugs/tasks — do not silently drop them.

## Acceptance Criteria
- [ ] All child implementation tasks (d2oj.1–.6) are closed with acceptance criteria verified by direct evidence
- [ ] Required tests/quality checks were re-run and passed
- [ ] Discovered defects are tracked as separate bugs/tasks (none silently dropped)
- [ ] Scope-level outcome matches the epic success criteria — each monitoring-data finding is fixed, mitigated, or explicitly accepted with rationale recorded
