---
id: bwb-00446
title: Replace loadgen theatre assertions with substantive ones
status: closed
type: chore
priority: 2
assignee: hans.kohlreiter@dynatrace.com
creator: Hans Kohlreiter
labels:
  - beads:beads-workbench-43th
parent: bwb-00101
created: 2026-05-25T18:46:26Z
updated: 2026-05-26T06:44:26Z
closed: 2026-05-26T06:44:26Z
close_reason: All acceptance criteria met (gate output and resolution captured in task comments). Cherry-picked onto main; mise run quality:fast and mise run test:integration both green on main after all 6 landed.
---

## Description
The `internal/testing/loadgen/` integration tests (~131s package wall time, dominant in `mise run test:integration`) are full of assertions that cannot fail by construction:

- `internal/testing/loadgen/measure_integration_test.go:97-103` — `MaxMs < 0`, `P50Ms < 0`, `P95Ms < 0`, `P99Ms < 0`
- `internal/testing/loadgen/generate_integration_test.go:41-43` — `ActualEdges < 0`
- `internal/testing/loadgen/measure_integration_test.go:155-168` — `TestMeasure_WriteReport` asserts the file is non-empty and starts with `{`, never decodes the payload

Wall-clock durations cannot be negative. Slice lengths cannot be negative. The substantive risks (percentile ordering, percentile↔Max relationship, report payload structure, SampleCount math) are not asserted.

Three E2E tests account for ~100s of the 131s package time:
- `TestMeasure_EndToEnd` (34s)
- `TestGenerate_EndToEnd` (30s)
- `TestGenerate_Determinism` (34s)

After stripping the always-true assertions, what remains is testable with a fake `bdrunner` in the unit suite.

## Instructions
1. Read both files end-to-end:
   - `internal/testing/loadgen/measure_integration_test.go`
   - `internal/testing/loadgen/generate_integration_test.go`
   - `internal/testing/loadgen/measure_test.go` (existing unit tests with NumPy-reference percentile math — extend this with the new fake-runner unit tests)
   - Production code: `internal/testing/loadgen/measure.go`, `generate.go`, the bdrunner interface
2. Remove every structurally-untriggerable assertion:
   - `X < 0` on durations, percentile values, slice lengths, counts
   - Tautologies like `SampleCount > 0` when `SamplesCold == opts.SamplesCold` is already asserted
3. Add substantive assertions in their place:
   - **Percentile ordering invariant** in both files where percentiles are computed: `P50Ms <= P95Ms`, `P95Ms <= P99Ms`, `P99Ms <= MaxMs`
   - **SampleCount math** — look up the actual sample-count formula in production code (likely `SamplesCold * <multiplier>` or similar) and assert it exactly. Do not just assert `>= SamplesCold`.
   - **`TestMeasure_WriteReport`** — decode the JSON file with `json.Unmarshal`, assert top-level shape (samples array, percentiles object, bd version field), and confirm the in-memory result matches the on-disk payload field-by-field
4. **REQUIRED — both halves** (this was tightened during plan-review; the original "OR" allowed skipping the substantive work):
   - **Convert** `TestMeasure_EndToEnd`, `TestGenerate_EndToEnd`, and `TestGenerate_Determinism` to fake-runner unit tests in `measure_test.go` / `generate_test.go`. The fake should return deterministic `time.Duration` values so percentile math is verifiable.
   - **AND** keep at most ONE real-bd E2E smoke test (≤15s wall-time budget) to cover bd-version capture end-to-end — that one risk is genuinely E2E. Pick whichever of the three test scenarios best exercises bd-version capture; convert the other two fully to fake-runner unit tests.
5. Run the full integration suite:
   ```
   mise run test:integration ./internal/testing/loadgen/...
   ```
   Measure wall time before/after. The new wall time should be ≤20s for the loadgen package (the ≤15s smoke + small fixed overhead).

## Files to Modify
- `internal/testing/loadgen/measure_integration_test.go`
- `internal/testing/loadgen/generate_integration_test.go`
- Possibly extend: `internal/testing/loadgen/measure_test.go`, `internal/testing/loadgen/generate_test.go`

## Acceptance Criteria
- [ ] Zero `< 0` assertions remain on durations, counts, or slice lengths in either file (`grep -n "< 0" internal/testing/loadgen/*_integration_test.go` returns no comparisons on duration/count types)
- [ ] Percentile ordering invariant (P50 ≤ P95 ≤ P99 ≤ Max) is asserted in both files
- [ ] `TestMeasure_WriteReport` decodes the JSON payload and asserts its shape and field values
- [ ] SampleCount math is asserted with the exact formula from production code, not just `> 0`
- [ ] At most ONE real-bd E2E smoke test remains in `internal/testing/loadgen/*_integration_test.go`; its wall-time budget is ≤15s
- [ ] The other two former E2E tests are converted to fake-runner unit tests (in `measure_test.go` / `generate_test.go`) with verifiable assertions
- [ ] `mise run test:integration ./internal/testing/loadgen/...` total wall time is ≤20s (down from baseline ~131s)
- [ ] `mise run test:integration ./internal/testing/loadgen/...` passes
- [ ] `mise run quality:fast` passes
