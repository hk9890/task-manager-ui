---
id: bwb-00485
title: 'Minor consistency cleanups: subprocess comments + NewModel variadic'
status: closed
type: chore
priority: 3
assignee: hans.kohlreiter@dynatrace.com
creator: Hans Kohlreiter
labels:
  - beads:beads-workbench-qash
parent: bwb-00110
created: 2026-05-22T07:50:17Z
updated: 2026-05-22T09:26:27Z
closed: 2026-05-22T09:26:27Z
close_reason: Minor cleanups complete; no functional change; build/test pass.
---

## Description

Two minor consistency cleanups. NO FUNCTIONAL CHANGE — a comment addition and a
behavior-preserving signature shape change.

## Instructions

1. Add a one-line rationale comment to each of the three subprocess-spawn sites
   explaining its design choice:
   - `internal/launcher/process_runner.go` — `exec.Command`, fire-and-forget
     (process must outlive bwb).
   - `internal/gateway/beads/runner.go` — `exec.CommandContext`, cancellable
     (gateway calls must respect timeout/shutdown).
   - `internal/launcher/editor/opener.go` — `exec.CommandContext`, interactive
     (wired to the user terminal).
2. Change `mode/board.NewModel`'s `resolved ...config.ResolvedKeyBindings`
   variadic parameter to a single non-variadic parameter, matching sibling
   constructors (`launcher.NewService`, `editor.NewIssueEditor`). Update all
   call sites accordingly. Behavior must be identical for the existing 0-or-1
   call patterns.
3. Run `mise run build` and `mise run test`.

## Files to Modify

- `internal/launcher/process_runner.go` (comment)
- `internal/gateway/beads/runner.go` (comment)
- `internal/launcher/editor/opener.go` (comment)
- `internal/mode/board/model.go` (signature) + its call sites

## Acceptance Criteria

- [ ] All three spawn sites carry a one-line rationale comment
- [ ] `mode/board.NewModel` no longer uses a variadic for the keybindings arg
- [ ] All call sites updated; `mise run build` and `mise run test` pass
- [ ] No functional change — behavior identical
