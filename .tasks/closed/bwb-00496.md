---
id: bwb-00496
title: Delete confirmed dead functions (5 items, zero callers)
status: closed
type: chore
priority: 2
assignee: hans.kohlreiter@dynatrace.com
creator: Hans Kohlreiter
labels:
  - area:cleanup
  - beads:beads-workbench-46vo
parent: bwb-00114
created: 2026-05-19T15:00:06Z
updated: 2026-05-19T15:26:32Z
closed: 2026-05-19T15:26:32Z
close_reason: '5 dead functions deleted (d14e074). All acceptance criteria met: greps clean, quality:fast green.'
---

## Description

Five functions have zero callers anywhere in the repo (production and tests). Verified via `grep -rn '\bFUNC\b' ... --include='*.go'`. Delete them and their comments.

## Files & symbols to delete

| File | Symbol | Line |
|---|---|---|
| `internal/gateway/beads/read_gateway.go` | `searchMetadataFromExactFilter` | 573 |
| `internal/ui/details/metadata.go` | `renderRelatedIssuesRail` | 223 |
| `internal/ui/shared/issuerow/row.go` | `RenderReferenceCompactSkeleton` | 104 |
| `internal/logging/logging.go` | `(*Manager).SessionID()` (method) | 111 |
| `internal/testing/ui/harness.go` | `AssertModelViewMatchesGoldenNormalized` | 100 |

Note: the `SessionID` *field* on the logging options struct is used heavily — only the method is dead. Do not touch the field.

## Instructions

1. For each symbol above: delete the function declaration plus its doc comment block.
2. Run `go build ./...` and `go vet ./...` after each removal.
3. Remove any now-unused imports surfaced by the build.
4. Run `mise run quality:fast` once all five are removed.

## Acceptance Criteria

- [ ] All 5 functions removed
- [ ] `grep -rn '\bsearchMetadataFromExactFilter\b' --include='*.go'` returns no hits
- [ ] `grep -rn '\brenderRelatedIssuesRail\b' --include='*.go'` returns no hits
- [ ] `grep -rn '\bRenderReferenceCompactSkeleton\b' --include='*.go'` returns no hits
- [ ] `grep -rn '\.SessionID()' --include='*.go'` returns no hits and `func.*SessionID()` returns no hits in logging.go
- [ ] `grep -rn '\bAssertModelViewMatchesGoldenNormalized\b' --include='*.go'` returns no hits
- [ ] `mise run quality:fast` passes
