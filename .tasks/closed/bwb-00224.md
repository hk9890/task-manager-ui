---
id: bwb-00224
title: Remove dead 'ActualEdges < 0' guard in loadgen/generate_test.go (post-j5bg follow-up)
status: closed
type: chore
priority: 3
assignee: hans.kohlreiter@dynatrace.com
creator: Hans Kohlreiter
labels:
  - test-quality-followup
  - beads:beads-workbench-99bs.4
created: 2026-05-26T06:57:48Z
updated: 2026-05-27T21:55:39Z
closed: 2026-05-27T21:55:39Z
close_reason: Dead ActualEdges<0 guard removed from generate_test.go. Substantive assertion preserved. quality:fast and test:integration both pass.
---

Verifier flagged a structurally-untriggerable guard in `internal/testing/loadgen/generate_test.go:387`:

```go
if m.ActualEdges < 0 {
    t.Fatalf("ActualEdges is negative: ...")
}
```

`ActualEdges` is set from `len(p.edges)` at `generate.go:301`, which returns a non-negative int by language spec. The in-file comment acknowledges this ("This cannot happen in practice (len is always ≥ 0)") and the substantive assertion follows at line 394 (`if m.ActualEdges != expectedEdges`).

The guard is dead code. The file is MODIFIED (not new) under 43th's refactor, so it passed the j5bg AC literal grep (which only checks NEW test files). Still worth removing to keep the test code honest with the epic's own "no assertions that cannot fail by construction" principle.

## Acceptance Criteria
- [ ] Remove the `if m.ActualEdges < 0 { ... }` guard at `generate_test.go:387`
- [ ] Verify the substantive `ActualEdges != expectedEdges` assertion still runs
- [ ] `mise run quality:fast` passes
- [ ] `mise run test:integration ./internal/testing/loadgen/...` passes

Low priority. Surfaced by j5bg verifier.
