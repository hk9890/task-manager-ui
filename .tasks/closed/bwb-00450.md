---
id: bwb-00450
title: Remove or fix scripts/verify_kb8e_manual_test.go hardcoded path
status: closed
type: chore
priority: 3
assignee: hans.kohlreiter@dynatrace.com
creator: Hans Kohlreiter
labels:
  - beads:beads-workbench-ku9t
parent: bwb-00101
created: 2026-05-25T18:46:43Z
updated: 2026-05-26T06:44:29Z
closed: 2026-05-26T06:44:29Z
close_reason: All acceptance criteria met (gate output and resolution captured in task comments). Cherry-picked onto main; mise run quality:fast and mise run test:integration both green on main after all 6 landed.
---

## Description
`scripts/verify_kb8e_manual_test.go:65` hardcodes `const bdBinary = "/home/hans/.local/share/mise/installs/github-gastownhall-beads/1.0.4/bd"`.

The file is build-tag-gated (`//go:build verify_kb8e`), so CI does not compile it. But the file is committed and the doc comment says *"Run with: `go test -tags=verify_kb8e -v ./scripts/...`"* — which no other developer can do.

`kb8e` is a CLOSED beads issue (verified during plan-review via `bd show beads-workbench-kb8e`: closed 2026-05-24, all 15 sub-issues PASS, acceptance review complete). The verify script is one-time verification for that closed epic — its 15 sub-test names map 1:1 to the closed kb8e.1–15 issues. The build-tag gating means only one developer can run it, so it provides zero ongoing value.

**Resolution: DELETE.** This was decided in the plan-review grill; do not re-litigate the keep-vs-delete question.

## Instructions
1. DELETE `scripts/verify_kb8e_manual_test.go`.
2. Grep for any remaining references to `verify_kb8e` (build tag, docs, scripts, mise tasks):
   ```
   grep -rn "verify_kb8e" --exclude-dir=.claude --exclude-dir=.git .
   ```
   Remove every dangling reference (build tags in CI configs, doc references, mise tasks, etc.).
3. Verify:
   ```
   go vet ./...
   mise run quality:fast
   ```

## Files to Modify
- DELETE: `scripts/verify_kb8e_manual_test.go`
- Possibly: documentation files or scripts that reference the `verify_kb8e` build tag (grep first)

## Acceptance Criteria
- [ ] `scripts/verify_kb8e_manual_test.go` is removed from the repo
- [ ] `grep -rn "verify_kb8e" --exclude-dir=.claude --exclude-dir=.git .` returns no matches in the active tree
- [ ] `grep -rn "/home/hans/" --exclude-dir=.claude --exclude-dir=.git .` returns no committed code matches (config/docs that intentionally reference user home are acceptable)
- [ ] `go vet ./...` clean
- [ ] `mise run quality:fast` passes
