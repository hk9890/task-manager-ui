---
id: bwb-00321
title: Enumerate every Dashboard(ctx) call site
status: closed
type: task
priority: 1
assignee: hans.kohlreiter@dynatrace.com
creator: Hans Kohlreiter
labels:
  - beads:beads-workbench-s4r7.1
parent: bwb-00019
created: 2026-05-24T18:07:11Z
updated: 2026-05-25T09:15:50Z
closed: 2026-05-25T09:15:50Z
close_reason: 'Enumeration complete: 96 call sites, 29 method definitions, 5 in scripts. Authoritative list in task description.'
---

Enumeration of every `.Dashboard(` call site that will need updating when `Repository.Dashboard` adds a `DashboardOptions` parameter.

Generated by: `grep -rn '\.Dashboard(' --include='*.go' . | grep -v '^./vendor/' | sort`
Date: 2026-05-25

---

**Summary: 96 call sites, 29 method definitions, 5 in scripts**
(6 production call sites, 85 test call sites, 5 script call sites)
(6 production method definitions, 22 test stub definitions, 1 script stub definition)

---

## CALL SITES (need to pass DashboardOptions{})

### Production (non-test)

- `cmd/bwb-smoke/main.go:551` — `runDashboardFetch`
- `internal/mode/board/model.go:554` — `loadDashboardCmd`
- `internal/repository/caching/caching.go:330` — Hydrate precompute (calls `loaded.Dashboard(ctx)` on memory.Repository)
- `internal/repository/caching/caching.go:435` — `CachingRepository.Dashboard` backing call (`c.backing.Dashboard(ctx)`)
- `internal/repository/errorinjecting.go:97` — `ErrorInjectingRepository.Dashboard` passthrough
- `internal/repository/validating.go:108` — `validatingRepository.Dashboard` passthrough

### Test files

**cmd/bwb/main_test.go** (4 call sites)
- `:619` — countingRepository.Dashboard delegates to wrapped repo (`r.Repository.Dashboard(ctx)`)
- `:668` — TestCachingModeStartupWiring first Dashboard call
- `:678` — TestCachingModeStartupWiring second Dashboard call
- `:783` — TestConstructRepositoryCachingWritesCacheFile

**internal/dashboard/parity/** (2 call sites)
- `count_parity_integration_test.go:51`
- `sort_parity_integration_test.go:148`

Note: `scale_parity_integration_test.go` has NO `.Dashboard(` call site.

**internal/mode/search/** (2 call sites inside stub method bodies)
- `enter_while_loading_test.go:44` — recordingSearchRepo.Dashboard body delegates to `r.repo.Dashboard(ctx)`
- `model_async_test.go:129` — DelayedFakeRepository.Dashboard body delegates to `d.inner.Dashboard(ctx)`

**internal/repository/caching/caching_test.go** (41 call sites)
- `:178`, `:190`, `:217`, `:223` — basic read/cache tests
- `:543`, `:558`, `:597`, `:608` — dirty-flag tests
- `:695`, `:715`, `:742`, `:761`, `:790`, `:807`, `:833`, `:850`, `:879`, `:898` — invalidation tests
- `:969`, `:983` — concurrent tests
- `:1055`, `:1064`, `:1088`, `:1102`, `:1134`, `:1150`, `:1192`, `:1207` — more cache tests
- `:1339`, `:1397` — concurrent contention tests
- `:1514`, `:1560` — additional tests
- `:1690`, `:1702` — cancel/error tests
- `:1944`, `:1981`, `:1992`, `:2022`, `:2107`, `:2140`, `:2169` — further tests

**internal/repository/caching/cold_start_test.go** (1 call site)
- `:48`

**internal/repository/contract/parity_integration_test.go** (10 call sites)
- `:275`, `:318`, `:363`, `:394`, `:423`, `:497`, `:529`, `:686`, `:704`, `:839`

**internal/repository/errorinjecting_test.go** (3 call sites)
- `:76`, `:143`, `:227`

**internal/repository/memory/repository_test.go** (6 call sites)
- `:119`, `:566`, `:657`, `:679`, `:701`, `:1012`

**internal/repository/validating_test.go** (16 call sites)
- `:158`, `:209`, `:246`, `:285`, `:318`, `:358`, `:389`, `:690`, `:724`, `:749`, `:773`, `:803`, `:833`, `:866`, `:906`, `:946`

## SCRIPTS / OTHER

**scripts/verify_kb8e_manual_test.go** (5 call sites)
- `:521` — call on `c`
- `:712` — call on `c`
- `:720` — call on `c`
- `:741` — call on `c`
- `:1102` — countingDashboardRepo.Dashboard body delegates to `c.Repository.Dashboard(ctx)`

---

## METHOD DEFINITIONS (need signature update)

### Production interface + implementations

- `internal/repository/repository.go:53` — `Repository` interface method
- `internal/repository/beads/lean_reads.go:42` — `beads.Repository`
- `internal/repository/memory/repository.go:308` — `memory.Repository`
- `internal/repository/caching/caching.go:419` — `caching.CachingRepository`
- `internal/repository/errorinjecting.go:89` — `ErrorInjectingRepository`
- `internal/repository/validating.go:107` — `validatingRepository`

### Test stubs (implement Repository interface, need signature update)

**internal/repository/validating_test.go** (16 stubs)
- `:95` — `stubRepository`
- `:194` — `emptyIDDashboardRepo`
- `:231` — `wrongSlotStatusRepo`
- `:261` — `overlapReadyBlockedRepo`
- `:300` — `readyTotalMismatchRepo`
- `:333` — `unenrichedBlockedByRepo`
- `:373` — `ssomViolationRepo`
- `:665` — `highCardDashboardRepo`
- `:709` — `emptyTitleDashboardRepo`
- `:735` — `emptyStatusDashboardRepo`
- `:760` — `emptyTypeDashboardRepo`
- `:788` — `wrongClosedSlotStatusRepo`
- `:818` — `wrongBlockedSlotStatusRepo`
- `:848` — `emptyReadyIDRepo`
- `:881` — `emptyBlockedIDRepo`
- `:921` — `blockedTotalMismatchRepo`

**Other test stubs**
- `internal/repository/caching/caching_test.go:58` — `stubRepository`
- `internal/repository/errorinjecting_test.go:20` — `fakeInner` (has `var _ repository.Repository = (*fakeInner)(nil)` compile-time check)
- `cmd/bwb/main_test.go:617` — `countingRepository`
- `internal/mode/search/enter_while_loading_test.go:43` — `recordingSearchRepo`
- `internal/mode/search/model_async_test.go:128` — `DelayedFakeRepository`

### Script stubs
- `scripts/verify_kb8e_manual_test.go:1100` — `countingDashboardRepo`

---

## FALSE POSITIVES (ruled out)

- `internal/mode/board/model_double_refresh_test.go:6` — comment only
- `internal/mode/board/model_test.go:63` — comment only
- `internal/mode/board/model_test.go:920` — comment only
- `scripts/verify_kb8e_manual_test.go:478` — comment line
- `scripts/verify_kb8e_manual_test.go:508` — inline comment text after `cancel()`
- `internal/repository/caching/caching_test.go:1648` — comment line
- `internal/repository/caching/caching_test.go:1673` — comment line
- `internal/mode/board/render_regression_test.go:95` — `func feedDashboard(m *Model, data repository.DashboardData)` — test helper function taking DashboardData, NOT a Repository.Dashboard implementation
- `internal/testing/e2e/embeddedfixture/fixture_integration_test.go` — confirmed phantom; file does not exist in the repo

---

## Notes for implementors

1. `caching.go:330` calls `loaded.Dashboard(ctx)` where `loaded` is a `*memory.Repository` (concrete type). After s4r7.3 updates `memory.Repository.Dashboard`, this line must pass `DashboardOptions{}` too.
2. The 22 test stub method definitions need signature updates to remain compilable; bodies can ignore the new parameter or pass it through.
3. `scale_parity_integration_test.go` — confirmed NO Dashboard call site (epic body was incorrect to list it).
4. `internal/mode/search/parity/` — confirmed no `.Dashboard(` calls.
