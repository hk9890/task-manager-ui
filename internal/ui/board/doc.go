// Package board renders the dashboard columns: headers, row windows, the age
// markers between rows, and the responsive width split that decides how many
// columns fit. EnsureVisible is the scroll contract a mode model calls so the
// offset it stores agrees with the lines Render draws.
package board
